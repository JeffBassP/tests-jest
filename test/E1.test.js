const { testeJest } = require("../E1");

// 1 (Obrigatório) ​ - Crie um teste para garantir que o Jest está funcionando normalmente em seu projeto.


test('teste de soma ', () => {
    expect(testeJest()).toBe(2);
    
});
